# Dev-сервер ходит в локальный бекенд из orenza-backend (docker, APP_PORT=8080).
# Origin http://localhost:3000 уже разрешён там в FRONTEND_URL и config/cors.php.
VITE_API_URL=http://localhost:8080/api/v1
